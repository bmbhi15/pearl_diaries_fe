EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key_here
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# CLERK_SECRET_KEY is a server-side credential — it does NOT belong in this
# app. It's used by a backend to call Clerk's Admin API. Set it only in the
# backend service's own environment/secrets manager, never here.
